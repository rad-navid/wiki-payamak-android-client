<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@null"
    android:orientation="vertical" >
   

    <ImageView
        android:id="@+id/categoryItemImageView"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_gravity="center"
        android:scaleType="fitCenter"
        android:src="@drawable/folder" >
    </ImageView>
    
     <CheckBox
        android:id="@+id/categorySelectCheckBox"
        android:layout_width="35dp"
        android:layout_height="35dp"
        android:layout_alignTop="@id/categoryItemImageView"
        android:layout_alignLeft="@id/categoryItemImageView" >
    </CheckBox>

    <TextView
        android:id="@+id/categoryItemNameTextView"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_below="@id/categoryItemImageView"
        android:layout_alignRight="@id/categoryItemImageView"
        android:layout_centerHorizontal="true"
        android:textColor="@color/background_dark" >
    </TextView>

</RelativeLayout>